name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Install
        run: uv sync
      - name: Tests
        run: uv run pytest
      - name: Ruff
        run: uv run ruff check .
      - name: Mypy
        run: uv run mypy src
